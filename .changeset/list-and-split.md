---
'@assforge/cogent-sandbox': patch
---

`image list` shows unreferenced local images with the prune hint, in
text and JSON. `workspace.ts` splits into lifecycle/instances/config/
state modules; no behaviour change.
